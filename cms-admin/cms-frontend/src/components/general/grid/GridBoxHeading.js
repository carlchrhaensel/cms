import React from 'react';

const GridBoxHeading = ({children}) => {
    return (
        <h1 className={'gridBoxHeading'}>
            {children}
        </h1>
    );
};

export default GridBoxHeading;
